export default function LearningPathPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-10">
      <h1 className="text-3xl font-semibold">User Learning Path</h1>
      <p className="mt-4 text-[var(--color-text-muted)]">
        This section will be defined later. The menu is live so it can be wired to a learning-path model without changing
        navigation.
      </p>
    </main>
  );
}
